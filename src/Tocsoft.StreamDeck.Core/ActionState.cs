namespace Tocsoft.StreamDeck
{
    /// <summary>
    /// The various states the actino can be in
    /// </summary>
    public enum ActionState
    {
        /// <summary>
        /// The defualt state (used when only one state is configured)
        /// </summary>
        Default = 0,
        /// <summary>
        /// The alternative state... could be off from on/off etc
        /// </summary>
        Alternative = 1
    }
}
